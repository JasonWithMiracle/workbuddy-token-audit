# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-20

首个公开版本。三大能力与完整工程化配套。

### 新增

**三种使用模式**（共享同一份价格表，不会出现两套价目）
- **audit** —— 实测审计：扫描本机请求级真实用量，按任务 / 工作空间 / 自动化三口径归因，
  输出 HTML + Markdown 报告与 5 张 CSV
- **dashboard** —— 交互式单文件看板（KPI / 日活热力图 / 0–24 时模型分布 / 每日模型占比 / 三级下钻）
- **plan** —— 任务内环节消耗预估（token 由 Agent 估算，用于事前规划）

**计价能力**
- **分时（峰谷）计价**：DeepSeek 系高峰价 = 空闲价 × 2，按**每条请求的时间戳**判定，
  支持横跨时段的任务
- **分段计价**：智谱 GLM 按输入长度分段（部分模型叠加输出长度档），按**每条请求实际长度**判档
- **缓存分档**：缓存命中、缓存写入、非缓存输入分别计价；不分档会把成本高估约 70%
- **节假日与调休**：按年维护，**调休上班日按工作日**处理
- **双时长口径**：墙钟跨度 + 活跃时长（剔除长空档）

**价格表可维护性**
- 价格表与日历**外部化**（`assets/price_table.json`、`assets/calendar_YYYY.json`）
- **当日首刷**：当日首次调用自动刷新，后续复用；`--force-refresh` 强制
- **混合抓取**：自动抓取官方页 + 关键词锚点校验；失败降级为人工待核实清单，**不静默跳过**
- `price_sources.json` 登记各厂商官方定价页，便于按固定路径核查

**工程化配套**
- `scripts/pricing_core.py` —— 计价纯函数模块，引擎 / 看板 / 测试共用同一实现
- `scripts/verify_cost.py` —— **独立实现**复算校验器，与引擎交叉验证
- `tests/` —— 46 个单元测试，覆盖价格匹配、峰谷判定（含调休日）、分段判档、缓存分档、数据契约
- `scripts/check_privacy.py` —— 提交前敏感信息扫描
- `--anonymize` —— 报告脱敏（任务名 / 工作空间名哈希化）
- `.github/workflows/ci.yml` —— 三平台 × 三 Python 版本 CI

### 已知限制

- 依赖客户端**未公开**的本地数据格式，官方调整即可能失效（非官方工具）
- 部分厂商定价页为 JS 渲染或需登录，无法自动抓取，稳定落入人工核实清单
- 金额为**按官方公开牌价的预估**，不是官方账单
- 额度（credit）与 token 量非线性，不作换算基准，仅作旁证

### 免责声明

本工具为**非官方工具**，与腾讯 / WorkBuddy 无任何关联，未获其授权或背书。
它仅解析用户**本机自有**的客户端数据，不发起任何上传。
